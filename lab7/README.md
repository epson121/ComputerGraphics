To use these examples you need to have glut installed
For linux:
```sh
sudo apt-get install freeglut3-dev
```

Compile with:
```sh
gcc GLConeRotation.c -lGLU -lglut -o cone.out
```
Execute
```sh
./cone.out
```

